# 赛事 E1 实现计划（TDD）

spec：`docs/superpowers/specs/2026-06-30-meets-e1-design.md`。分支 `feat/meets-e1`。

## Task 1 — shared 类型
- `Gender/Stroke/ResultStatus` 联合类型；`MeetSummary/MeetDetail/RaceEventItem/EntryItem/StandingRow/StandingsGroup`；`CreateMeetDto/CreateRaceEventDto/CreateEntryDto/SetResultDto`。
- `SwimmerListItem` + `CreateSwimmerDto` 加 `gender?/birthDate?`。
- `npm run build -w @swim/shared`。

## Task 2 — Prisma 模型 + 迁移
- 枚举 Gender/Stroke/ResultStatus；`User += gender?/birthDate?`；`Meet/RaceEvent/MeetEntry`（FK 见 spec）。
- 手写迁移 `20260630020000_add_meets`：CREATE TYPE ×3、ALTER User ADD ×2、CREATE TABLE ×3 + 索引 + FK。
- generate + dev migrate deploy。

## Task 3 — 名次/时间/年龄组纯函数（红→绿）
- `meets/age-group.ts`：`AGE_GROUPS` + `ageGroupOf(birthDate, meetDate)`。
- `meets/swim-time.ts`：`parseSwimTime('1:02.34')→ms`、`formatSwimTime(ms)→'1:02.34'`。
- `meets/standings.ts`：`computeStandings(entries, meetDate)` → `StandingsGroup[]`。
- 各 `.spec.ts`：年龄边界、时间往返、分组/并列/奖牌/排除非 OK。

## Task 4 — billing feature + 会员人口学
- `plan.config.ts` features 加 `meets`（FREE false/PRO true）；`assertFeature` 联合加 `'meets'` + label「赛事」。
- 名册：`CreateSwimmerDto`(service) 加 gender/birthDate；createSwimmer 落库；`UpdateMembershipDto` 或新 `UpdateSwimmerDto` 支持改 gender/birthDate；`PATCH /pools/:id/swimmers/:sid` 接受之。补单测。

## Task 5 — MeetsService + 控制器（红→绿）
- `meets/meets.service.ts`：createMeet(Pro 门禁)/listMeets/meetDetail/deleteMeet；addEvent/deleteEvent；addEntry(校验 owner+人口学)/listEntries/deleteEntry；setResult；standingsOf（拼 computeStandings + formatSwimTime）。所有权助手。
- `meets/meets.controller.ts`：spec 端点。
- `meets/meets.module.ts`：imports BillingModule；providers [MeetsService, PrismaService]。
- `app.module.ts` imports MeetsModule。
- `meets.service.spec.ts`（mock prisma+billing）：建赛门禁、报名缺人口学 422、setResult、standings 组装、所有权。
- 跑 api 单测全绿。

## Task 6 — e2e
- `test/meets-flows.e2e-spec.ts`：owner(PRO)建赛→加项目(50 FREE)→建会员设 gender/birthDate→报名→录成绩(2 人同组)→`GET standings` 断言金/银 + 名次；DNS 不计；Free 建赛 402；报名缺人口学 422。
- beforeAll 清库（含 meetEntry/raceEvent/meet/apiKey/idempotencyKey）。
- 跑 e2e 全绿。

## Task 7 — 前端
- endpoints/queries：meets CRUD、events、entries、result、standings；会员 gender/birthDate。
- 顶层「赛事」菜单（AppLayout）+ 路由 `/meets`、`/meets/:id`。
- `MeetsListPage`（列表+新建）、`MeetDetailPage`（项目→报名→录成绩→排行榜分组+奖牌）。
- 名册 `CreateSwimmerModal` + 编辑加 性别/出生日期。
- web msw 默认 handlers；测试（赛事详情流 + 排行榜渲染 + 名册人口学）。
- 跑 web 单测全绿。

## Task 8 — 终验
- 全量 lint/build/test/e2e；README 增「赛事」段 + 端点表；自评审；合并 main + 推送 + 删分支；通知验收。
