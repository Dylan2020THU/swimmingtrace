services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: swim
      POSTGRES_PASSWORD: swim
      POSTGRES_DB: swim
    ports:
      - "5432:5432"
    volumes:
      - swim_pgdata:/var/lib/postgresql/data

volumes:
  swim_pgdata:
